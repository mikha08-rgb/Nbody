{
  "name": "nbody-sim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "2D N-body gravity simulator — Phase 1: brute-force CPU baseline",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "tsx scripts/bench.ts",
    "test:gpu": "vitest run --config vitest.gpu.config.ts",
    "bench:gpu": "tsx scripts/bench-gpu.ts"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "^4.1.9",
    "@webgpu/types": "^0.1.71",
    "playwright": "^1.61.1",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  }
}
